
Python :

Module: requests:

	- arg = url, headers(op)

	samples:
		- requests.get(url="", headers = headers
			, params = {key:value}
			, data = {inputTag:value, ..}) # a = request.post(url = "",data = {} -method to post
			, file = {input -> name : open(file, "rb")}
			, timeout=5 # if take response more than 5 sec it will through error
			)


	download:
		a = request.get(url = "")
		img = a.content
		file = open("file.type", "wb")
		file.write(img)

	Methods:
		- 	a = request.get(url = "")
			a.text # return string of the html
			a.url  # give the same url as expected
			
	
Module: SimpleHTTPServer

	- enable : python -m SimpleHTTPServer port(op)
		python -m SimpleHTTPServer 8080
	


