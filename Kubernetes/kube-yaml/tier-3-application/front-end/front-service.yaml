apiVersion: v1
kind: Service
metadata:
  name: front-service
  labels:
    app: front-service
spec:
  type: NodePort
  selector:
    app: front-deployment-pod
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30081